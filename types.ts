
export interface Service {
  id: string;
  title: string;
  description: string;
  icon: string;
}

export interface BeforeAfterImage {
  id: number;
  before: string;
  after: string;
  title: string;
}
